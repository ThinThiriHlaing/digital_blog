---
import BaseHead from "../components/BaseHead.astro";
import Header from "../components/Header.astro";
import Footer from "../components/Footer.astro";
import BlogPostDetail from "../components/BlogPostDetail.astro";
import { SITE_TITLE, SITE_DESCRIPTION } from "../consts";
import "../styles/style.css";
import Blogtemplate from "../components/Blogtemplate.astro";

const { content } = Astro.props;
const { img, description, paragraph } = content;

// console.log("Thin");
// console.log(Astro.props);

const blog = await Astro.glob("../pages/blog/*.{md,mdx}");
console.log(blog);
console.log("ma ma");

const filter = () => {
  const filterBlog = blog.filter(
    (blogs) => blogs.frontmatter.id == blogs.frontmatter.id
  );
  return filterBlog;
};
filter();
---

<!DOCTYPE html>
<html lang="en">
  <head>
    <BaseHead title={SITE_TITLE} description={SITE_DESCRIPTION} />
    <link
      rel="stylesheet"
      href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.3.0/css/all.min.css"
      integrity="sha512-SzlrxWUlpfuzQ+pcUCosxcglQRNAq/DZjVsC0lE40xsADsfeQoEypE+enwcOiGjk/bSuGGKHEyjSoQ1zVisanQ=="
      crossorigin="anonymous"
    />
    <link rel="stylesheet" href="../styles/style.css" />
    <script src="https://identity.netlify.com/v1/netlify-identity-widget.js"
    ></script>
  </head>
  <body class="bg-[#02020b] overflow-x-hidden container-fluid">
    <Header />
    <BlogPostDetail {img} {description} {paragraph}>
      <slot />
    </BlogPostDetail>
    <!-- {
      blog &&
        blog.filter((blog) => blog.frontmatter.id == id
          // console.log(blog.frontmatter.id);
          // console.log("mg mag");
        )
    } -->
    <Footer />
  </body>
</html>
