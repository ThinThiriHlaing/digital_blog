---
const { date } = Astro.props;
console.log(date);
---

<time datetime={date}>
  {
    new Date(date).toLocaleDateString("en-us", {
      year: "numeric",
      month: "short",
      day: "numeric",
    })
  }
</time>
